#include "main.h"

/**
 * add- Adds two integers and returns the result
 * @x: One of the integers
 * @z: One of the integers
 * Return: Sum of two integers
 */

int add(int x, int z)
{
	int sum;

	sum = x + z;
	return (sum);
}
