#include "operations.h"

/**
 * _mod- Finds the remainder of a quotient of two integers
 * a: First integer
 * b: second integer
 *
 * Return: The remainder
 */

int mod(int a, int b)
{
	return (a % b);
}
